import { CadenaEnMayusculasDirective } from './cadena-en-mayusculas.directive';

describe('CadenaEnMayusculasDirective', () => {
  it('should create an instance', () => {
    const directive = new CadenaEnMayusculasDirective();
    expect(directive).toBeTruthy();
  });
});
